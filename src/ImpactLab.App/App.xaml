<Application
    x:Class="ImpactLab.App.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    StartupUri="MainWindow.xaml"
>
    <Application.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Margin" Value="0,4,0,4" />
            <Setter Property="Background" Value="#28384B" />
            <Setter Property="Foreground" Value="#F1F5FA" />
            <Setter Property="BorderBrush" Value="#4B6077" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,3,0,7" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="Background" Value="#E8EEF5" />
            <Setter Property="Foreground" Value="#1B2633" />
            <Setter Property="BorderBrush" Value="#4B6077" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="0,3,0,7" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="Background" Value="#E8EEF5" />
            <Setter Property="Foreground" Value="#1B2633" />
            <Setter Property="BorderBrush" Value="#4B6077" />
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#D8E0EA" />
        </Style>
    </Application.Resources>
</Application>
